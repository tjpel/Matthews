{
  "builds": [
    {
      "src": "/.*\\.py",
      "use": "@vercel/python",
      "config": {
        "cmd": "poetry install && uvicorn app.main:app --host 0.0.0.0 --port 8080"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app/main.py"
    }
  ]
}
